<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:background="@drawable/bg_gradient_aliceblue"
    android:gravity="top|center_horizontal">

    <!-- Cabeçalho / Logo mantendo identidade PTP-1 -->
    <TextView
        android:id="@+id/logoTitle"
        android:text="PTP-1"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@color/primary"
        android:fontFamily="sans-serif-black"
        android:gravity="center"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <!-- Barra de ações: botão Adicionar (único) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="12dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAdd"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Adicionar"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:fontFamily="sans-serif-medium"
            app:icon="@drawable/ic_add_24"
            app:iconTint="@android:color/white"
            app:iconGravity="textStart"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            app:cornerRadius="12dp"
            android:backgroundTint="@color/buttonGradientStart"
            style="@style/Widget.MaterialComponents.Button"/>
    </LinearLayout>

    <!-- Container da lista com canto arredondado e sombra sutil -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingTop="4dp"
        android:paddingBottom="8dp">

        <ListView
            android:id="@+id/listView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@android:color/transparent"
            android:listSelector="@drawable/list_item_selector"
            android:padding="8dp"
            android:clipToPadding="false"/>
    </FrameLayout>

    <!-- Texto de ajuda discreto -->
    <TextView
        android:id="@+id/tvHelp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Toque em Adicionar para criar um novo item"
        android:textSize="13sp"
        android:textColor="@color/accent"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="6dp"/>
</LinearLayout>
